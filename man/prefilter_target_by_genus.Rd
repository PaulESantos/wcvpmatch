% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prefilter_target_by_genus.R
\name{prefilter_target_by_genus}
\alias{prefilter_target_by_genus}
\title{Prefilter Target Backbone by Input Genera (Exact + Fuzzy)}
\usage{
prefilter_target_by_genus(
  df,
  target_df = NULL,
  genus_index = NULL,
  include_fuzzy = TRUE,
  max_dist = 1,
  method = "osa"
)
}
\arguments{
\item{df}{Input tibble/data.frame with either \code{Genus}/\code{Species} or \code{Orig.Genus}/\code{Orig.Species}.}

\item{target_df}{Optional custom target table; if \code{NULL}, uses \code{rWCVPdata::wcvp_names}.}

\item{genus_index}{Optional pre-built index from \code{build_genus_index()}. If \code{NULL}, it is built on the fly.}

\item{include_fuzzy}{Logical. If \code{TRUE}, include fuzzy-matched genera.}

\item{max_dist}{Maximum fuzzy distance for genus matching (used when \code{include_fuzzy = TRUE}).}

\item{method}{String distance method passed to \code{fozziejoin}.}
}
\value{
A prefiltered \code{target_df} tibble compatible with \code{wcvp_matching(target_df = ...)}.
Attributes:
\describe{
\item{candidate_genera}{Character vector of selected genera.}
\item{exact_genera}{Character vector of exact matched genera.}
\item{fuzzy_genera}{Character vector of fuzzy matched genera.}
}
}
\description{
Reduces the target backbone to genera relevant for the current input names.
This is designed as a pre-step before \code{wcvp_matching()} to reduce search space.

Strategy:
\itemize{
\item Exact genus candidates are always included.
\item Optional fuzzy genus candidates are included when \code{include_fuzzy = TRUE}.
\item Returned object preserves the standard target schema used by the package.
}
}
