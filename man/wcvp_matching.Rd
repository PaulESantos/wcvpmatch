% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/matching.R
\name{wcvp_matching}
\alias{wcvp_matching}
\title{Match Scientific Names Against WCVP}
\usage{
wcvp_matching(
  df,
  target_df = NULL,
  prefilter_genus = FALSE,
  allow_duplicates = FALSE,
  max_dist = 1,
  method = "osa"
)
}
\arguments{
\item{df}{Input tibble/data.frame with either \code{Genus}/\code{Species} or
\code{Orig.Genus}/\code{Orig.Species}. For trinomials, include \code{Infra.Rank} and
\code{Infraspecies} (or \code{Orig.Infra.Rank}/\code{Orig.Infraspecies}).}

\item{target_df}{Optional custom target table. If \code{NULL}, data are read from
\code{rWCVPdata::wcvp_names}.}

\item{prefilter_genus}{Logical. If \code{TRUE}, prefilter \code{target_df} to candidate
genera (exact + fuzzy) before running the matching pipeline.}

\item{allow_duplicates}{Logical. If \code{TRUE}, duplicated taxon keys are
deduplicated internally for matching and then expanded back to original
rows. Output includes \code{input_index} for traceability to the original input.}

\item{max_dist}{Maximum distance used in all fuzzy matching stages (genus, species, infraspecies).}

\item{method}{A string indicating the fuzzy matching method (passed to \code{fozziejoin}). Supported methods:
\itemize{
\item \code{"levenshtein"}: Levenshtein edit distance (default).
\item \code{"osa"}: Optimal string alignment.
\item \code{"damerau_levensthein"} or \code{"dl"}: Damerau-Levenshtein distance.
\item \code{"hamming"}: Hamming distance (equal-length strings only).
\item \code{"lcs"}: Longest common subsequence.
\item \code{"qgram"}: Q-gram similarity (requires \code{q}).
\item \code{"cosine"}: Cosine similarity (requires \code{q}).
\item \code{"jaccard"}: Jaccard similarity (requires \code{q}).
\item \code{"jaro"}: Jaro similarity.
\item \code{"jaro_winkler"} or \code{"jw"}: Jaro-Winkler similarity.
\item \code{"soundex"}: Soundex codes based on the National Archives standard.
}}
}
\value{
Tibble with matched names, process flags, and taxonomic context
columns: \code{matched_plant_name_id}, \code{matched_taxon_name}, \code{taxon_status},
\code{accepted_plant_name_id}, \code{accepted_taxon_name}, \code{is_accepted_name}.
}
\description{
Runs a matching pipeline with exact and partial matching for binomial and
trinomial names, including infraspecific rank validation.
}
